# Third-party notices

## Qwen3-TTS software and model files

The Qwen team publishes the upstream Qwen3-TTS runtime and the
`Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice` model. Setup retrieves the model from
the immutable revision `0c0e3051f131929182e2c023b9537f8b1c68adfe` and verifies
the complete declared inventory. The upstream software and model retain their
Apache License 2.0 terms. See `LICENSES/Apache-2.0.txt` and the upstream model
card.

## Runtime dependencies

The extension installs pinned Python packages into its isolated `venv` during
Modly setup. Those packages retain their respective upstream licenses and are
not relicensed by the MIT license covering this integration.

## Modly

Modly is the host application by Lightning Pixel. It is not redistributed or
relicensed by this repository.
